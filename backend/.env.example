# Platform Cloud Backend - Environment Variables
# Copy this file to .env and fill in the values

# Server Configuration
PORT=3000

# MongoDB Connection
# If using Azure Key Vault, this will be retrieved from Key Vault
# Otherwise, set this variable
MONGODB_URI=mongodb://admin:password@localhost:27017/platform-cloud?authSource=admin

# Azure Key Vault Configuration (Optional)
# If set, secrets will be retrieved from Key Vault
# Uses DefaultAzureCredential for authentication
AZURE_KEY_VAULT_URL=https://your-keyvault-name.vault.azure.net/

# Azure Blob Storage Configuration
# If using Azure Key Vault, this will be retrieved from Key Vault (STORAGE-CONNECTION-STRING)
# Otherwise, set this variable directly
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your-account;AccountKey=your-key;EndpointSuffix=core.windows.net

# Azure Blob Container Name (default: 'images')
AZURE_BLOB_CONTAINER_NAME=images

# JWT Secret for Authentication
JWT_SECRET=your-secret-key

# ===========================================
# Azure Key Vault Secret Names:
# - MONGO-CONNECTION-STRING: MongoDB connection string
# - STORAGE-CONNECTION-STRING: Azure Blob Storage connection string
# ===========================================

